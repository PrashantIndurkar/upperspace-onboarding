/**
 * Theme color values for use in JS (e.g. icon color props, placeholderTextColor).
 * Keep in sync with tailwind.config.ts theme.extend.colors.
 */
export const colors = {
  primary: "#e7f160",
  overlay: "rgba(0,0,0,0.5)",
  overlayLight: "rgba(0,0,0,0.4)",
  icon: "#171717",
  iconMuted: "#525252",
  placeholder: "#a3a3a3",
} as const;
